# Copy to .env and fill in real values:
#   cp .env.example .env

APP_MODE=live
PORT=4000

DB_HOST=127.0.0.1
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=password123
DB_NAME=signal_ledger

# Sample mode metadata is injected by the sample Docker profile.
# SAMPLE_DATASET_VERSION=
# SAMPLE_DATA_AS_OF=
# SAMPLE_TICKER_COUNT=
