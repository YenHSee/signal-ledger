# Copy to .env and fill in real values:
#   cp .env.example .env
#
# NOTE: DB settings are still hardcoded in src/app.module.ts for local dev.
# Before production deploy, wire these env vars into TypeORM config.

PORT=4000

DB_HOST=127.0.0.1
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=password123
DB_NAME=stock_analyst
