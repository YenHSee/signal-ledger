# 🏢 Project Context (金融智能投研平台)

你现在协助一位拥有 8 年经验的资深前端专家（正在转型全栈/架构师）。

本项目是一个企业级的 AI 金融分析终端。

- 前端：React 18+, React Router, Tailwind CSS, 极客风 UI。

- 后端：NestJS, TypeORM, PostgreSQL (包含 JSONB 字段处理)。

- AI 数据引擎：Python, LangGraph, Pydantic Schema, OpenAI/DeepSeek API。



# ⚠️ 核心开发纪律 (致命规则)

1. **绝不擅自做主**：在修改核心架构（如 Entity、Module 引入、路由）之前，必须先用文字向我解释你的思路，等我确认后再给出代码。

2. **遵守现有架构**：严格区分后端的 Data Access Layer (Entity) 和 Data Transfer Object (DTO/Schema)。不要混淆它们。

3. **金融级严谨性**：涉及金额、PE Ratio、涨跌幅等数据，必须做好防空（null/undefined）和兜底渲染处理。

4. **小步快跑**：不要一次性给我几百行的完整文件替换。只给我需要修改的代码块，并标明插入位置。

5. **语言要求**：用中文与我交流，但代码注释和变量名必须是专业的英文。

# 📂 Monorepo 架构边界 (极度重要)
本项目采用 Monorepo 结构，各模块物理隔离，职责严格划分。在跨目录写代码时必须遵守以下边界：

1. `api-types`: 前后端共享的 TypeScript 类型定义、DTO 和接口契约。
   - **规则**：必须保持绝对纯粹，只能包含 `type` 和 `interface`。绝不能包含任何 Node.js 或浏览器特定的运行时依赖。

2. `backend-node`: 基于 NestJS 的 BFF (Backend for Frontend) 服务。
   - **规则**：负责业务逻辑、提供 REST/WebSocket API、读取数据库。它可以 import `api-types`，但**绝对禁止**引入 `frontend-web` 的代码。

3. `frontend-web`: 基于 ReactJS 的交易终端前端。
   - **规则**：负责 UI 渲染。它可以 import `api-types`，但**绝对禁止**引入 `backend-node` 或 `data-python` 的任何文件。

4. `data-python`: 基于 Python 的底层量化/AI 引擎。
   - **规则**：这是一个独立的数据流水线。负责定时拉取金融数据、调用大模型生成研报，并直接存入 PostgreSQL 数据库供后端读取。