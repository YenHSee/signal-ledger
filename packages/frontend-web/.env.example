# Copy to .env and fill in real values:
#   cp .env.example .env
#
# NOTE: API URL is still hardcoded in src/pages/stock/** for local dev.
# Before production deploy, read import.meta.env.VITE_API_BASE in fetch calls.

# Backend API base URL (no trailing slash)
VITE_API_BASE=http://localhost:4000/api
